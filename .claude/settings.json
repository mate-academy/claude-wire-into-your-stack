{
  "permissions": {
    "allow": [
      "mcp__fetch__fetch"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path // empty'); case \"$file\" in *.js) cd \"$CLAUDE_PROJECT_DIR\" && out=$(npm run lint 2>&1); code=$?; if [ \"$code\" -ne 0 ]; then echo \"$out\" >&2; exit 2; fi ;; esac"
          }
        ]
      }
    ]
  }
}
