---
description: List image files in docs/, each with a short one-line description of what it shows
---
Use `mcp__filesystem__list_directory` to find image files (`.png`, `.jpg`, `.jpeg`, `.gif`, `.svg`, `.webp`) in the project's `docs/` folder. For each one, use the `Read` tool to view it, then report the filename with a short, accurate one-line description of what it shows.
