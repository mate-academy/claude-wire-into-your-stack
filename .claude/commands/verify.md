---
description: Run the test suite and linter, and summarize any failures
---

Run these two checks on the project, in order:

1. `npm test`
2. `npm run lint`

For each command:
- If it passes, say so in one line (e.g. "tests: 8 passed").
- If it fails, don't just paste the raw output — pull out each failing test/lint rule, the file and line involved, and a one-line reason. Then suggest the smallest fix.

Stop after reporting; don't make any code changes unless I ask you to.
