{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "."]
    }
  },
  "_note": "In .claude/settings.json, the filesystem server is scoped to read-only tools (read_text_file, read_multiple_files, list_directory, list_directory_with_sizes, directory_tree, search_files, get_file_info, list_allowed_directories) — no write_file, edit_file, move_file, or create_directory."
}
