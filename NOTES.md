Connected the filesystem MCP server (`docs`) scoped to `./docs`, since `docs/api.md` is the source of truth for request/response shapes and letting Claude read it directly keeps generated routes/tests/docs consistent. The permission rule only allows its read tools (`read_text_file`, `list_directory`, `search_files`, etc.), not `write_file`/`edit_file`, since Claude should edit `docs/` normally, not through the server. Used it for real: asked headless Claude to check `docs/api.md` for a DELETE endpoint, and it read the file and answered correctly.

The skill captures this repo's test convention: `node:test` + `supertest`, `store.reset()` in `beforeEach`, one test per behavior, asserting status and body, covering the success/400/404 cases from `docs/api.md`. Scoped the description to route-testing requests specifically so it doesn't fire on unrelated test questions. Confirmed it by asking (without naming it) to add tests for `GET /health`. It fired, and `tests/health.test.js` is the result. The `/new-route` command scaffolds a whole new resource (store helpers, router, mount in `server.js`, docs section) since that's the most repeated multi-file task here and easy to half-do. Ran it once on a throwaway `widget` resource. It got all four pieces right and passed lint/tests. Then I reverted both the new route and new test changes since this branch is about the wiring, not a new feature.

The hook runs lint on any `.js` file after Edit/Write and feeds violations back to Claude via exit code 2, so the standard from `npm run lint`/CI gets caught immediately instead of later. Originally tried `eslint --fix`, but this repo's ESLint config (`eslint:recommended` + one custom rule) has no stylistic rules, so `--fix` had nothing to do; switched to surfacing violations instead of pretending to auto-fix. Triggered it on purpose with an unused-var file (fired, exit 2) and a clean file (stayed silent). Last, ran `/new-route widget (fields: name, price)` headless via `claude -p ... --allowedTools "Read,Write,Edit"`, confirmed locked to just those three tools, no `Bash`, so it could do exactly what the command needs and nothing else unattended.
