MCP server - notion

Skill - reviewing my cardio session

Command - Review a pull request for a specific set of concerns argument-hint: [pr-number] [focus-area]

Hook - A PreToolUse hook that blocks a dangerous Bash command — useful since it ties directly into the plan-mode/deny-mode material:

Task - Summarize today's cardio session against recent history and append the summary to today's Journal entry — read from `DB:Cardio`, cross-reference `DB:Cardio` history, write to `DB:Journal`. Nothing else.